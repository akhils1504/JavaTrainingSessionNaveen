package Interface;

public interface UsBankInterface {
	int min_bal=100;
	void credit();
	void debit();
	void transferMoney();
	//only method declaration
	//no method body ---only method prototype
	//in interface we can declare the variable and the variables re by default static in nature 
	//Variable values will not be changed
	//no static method in interface
	//no main method in interface
	//cant create the object of an interface
	//interface is abstract in nature
	

}
